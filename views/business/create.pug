extends ../layouts/common

block content
    h1 Create new book
    form(action="/shops/books/create", method="POST")
        input(type="text", name="businessId", value = id)
        label Name
        input(type="text", name="bookName")
        span &nbsp
        label Description
        input(type="text", name="bookDescription")
        button(class="btn btn-primary ml-2") Create
    